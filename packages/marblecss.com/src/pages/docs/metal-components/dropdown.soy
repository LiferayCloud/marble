---
description: "Create description."
title: "Dropdown"
---

{namespace pageMetalComponentsDropdown}

/**
 *
 */
{template .render}
	{let $dropdown kind="html"}
		<h5>Dropdown Settings</h5>
		{call MarbleDropdown.render }
			{param type: 'settings' /}
		{/call}

		<h5>Dropdown Filter</h5>
		{call MarbleDropdown.render }
			{param type: 'filter' /}
		{/call}

	{/let}

	{call styleguide.render data="all"}
		{param content: $dropdown /}
	{/call}
		
{/template}
